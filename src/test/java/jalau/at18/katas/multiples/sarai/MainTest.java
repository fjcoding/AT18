package jalau.at18.katas.multiples.sarai;
import static org.junit.Assert.*;
public class MainTest {
    public void shouldReturnAdd() {
        String input = "25";
        String expected = "the sum of the numbers is: 143";
        assertEquals(expected, input);
        assertEquals("the sum of the numbers is: 23", 10);
        assertEquals("the sum of the numbers is: 0 ", 0);
    }
}
