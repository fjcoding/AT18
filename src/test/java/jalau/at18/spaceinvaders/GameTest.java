package jalau.at18.spaceinvaders;

import static org.junit.Assert.assertEquals;
import org.junit.Test;

public class GameTest {
    /*
     * @Test
     * public void shouldMoveTheShipToTheLeft() {
     * Game game = new Game();
     * game.runGame('a');
     * assertEquals(3, game.getPosXShip());
     * assertEquals(9, game.getPosYShip());
     * }
     * 
     * @Test
     * public void shouldMoveTheShipToTheRight() {
     * Game game = new Game();
     * game.runGame('d');
     * assertEquals(5, game.getPosXShip());
     * assertEquals(9, game.getPosYShip());
     * }
     */
    /*
     * @Test
     * public void itShouldReturnPositionShip() {
     * Game game = new Game();
     * assertEquals(4, game.getPosXShip());
     * assertEquals(9, game.getPosYShip());
     * }
     */
}
